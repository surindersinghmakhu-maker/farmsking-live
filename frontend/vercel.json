{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps && CI=1 npx expo export -p web",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
