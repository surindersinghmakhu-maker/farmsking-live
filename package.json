{
  "name": "farmsking-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --legacy-peer-deps && npx prisma generate && npx prisma db seed && npm run build",
    "start": "cd backend && npx prisma db seed && node dist/main",
    "build:web": "node build.js"
  }
}
