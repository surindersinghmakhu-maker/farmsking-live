{
  "name": "farmsking-monorepo",
  "version": "2.0.1",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --legacy-peer-deps && npx prisma@6.4.0 generate && npm run build",
    "start": "cd backend && node dist/main.js",
    "build:web": "node build.js",
    "deploy:hostinger": "node build.js && git add . && git commit -m \"deploy: hostinger live update\" && git push origin main"
  }
}
