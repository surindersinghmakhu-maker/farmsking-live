services:
  - type: web
    name: farmsking-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install --legacy-peer-deps && npx prisma@6.4.0 generate && npm run build
    startCommand: npx prisma@6.4.0 migrate deploy && npx prisma@6.4.0 db seed && npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_PREFIX
        value: /api/v1
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: farmsking-db
          property: connectionString

databases:
  - name: farmsking-db
    databaseName: farmsking
    user: nestuser
    plan: free
